# Versão segura

Esta versão mantém a mesma interface do laboratório original, mas usa consulta preparada e `password_hash()` / `password_verify()`.

Acesse enquanto o Docker estiver rodando:

```text
http://localhost:8081/secure/
```

Login: `marina` / `vibe2026`.
